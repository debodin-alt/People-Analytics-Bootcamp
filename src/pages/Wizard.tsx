import { Placeholder } from './Placeholder';

export function Wizard() {
  return <Placeholder title="Wizard" note="Conversational analyst over the semantic layer — built once the metrics views exist." />;
}
