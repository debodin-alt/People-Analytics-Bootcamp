import { Placeholder } from './Placeholder';

export function AdminUpload() {
  return <Placeholder title="Admin: Data Upload" note="Drop files, preview detected header/columns, validate, confirm to load." />;
}
